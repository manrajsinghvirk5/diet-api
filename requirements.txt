# Note: Do not list azure-functions here — the Functions host supplies it; duplicating it in the
# deployment package can cause version conflicts on Linux consumption/premium apps.
azure-storage-blob>=12.19.0
pandas>=1.3.0
numpy>=1.21.0
bcrypt>=4.0.0
PyJWT>=2.8.0
redis>=5.0.0
